# 基本面模块路线图

这份文档描述基本面模块的落地顺序，避免一开始就把数据源、规则引擎、CLI、可视化、消息推送全部揉在一起。

如果阅读过程中需要回到总导航，见 [fundamental-doc-map.md](fundamental-doc-map.md)。

这份文档更适合放在“规格已经看完，准备排实现顺序”时再读。建议前置阅读：

- [fundamental-module-spec.md](fundamental-module-spec.md)
- [fundamental-v1-minimum-fields.md](fundamental-v1-minimum-fields.md)
- [fundamental-data-source.md](fundamental-data-source.md)

## 1. 总体原则

- 先文档，后实现
- 先标准模型，后具体数据源
- 先单标的分析，后批量筛选
- 先规则化评分，后复杂估值模型
- 先可解释文本输出，后图表与自动推送

## 2. 分阶段目标

### 阶段 1：规格冻结

交付：

- 基本面模块设计文档
- 行业分层规则文档
- 科技行业子模型文档
- 领域模型定义
- 打分规则
- 风险标记规则
- README 文档入口整理

完成标准：

- 对 `FundamentalSnapshot` 字段有统一定义
- 对总分、维度分、评级口径有统一定义
- 对行业分层边界有明确约定
- 对技术面与基本面的组合方式有明确边界

### 阶段 2：最小实现闭环

交付：

- 标准快照数据结构
- 评分引擎
- 文本报告输出
- 单元测试

当前仓库状态：这一阶段已经基本落地，`src/fundamental/models/`、`scoring/`、`reporting/`、`validation/`
以及对应测试已具备第一版闭环。

更完整的当前能力快照，见 [fundamental-doc-map.md](fundamental-doc-map.md) 的“当前实现快照”章节。

状态补充：

- 文本报告当前已不只输出“总分 + 评级 + 优势风险”
- 还会输出维度得分的简版计算依据；按当前第一版实现，维度内仍是“已计分子项做平均，再乘维度权重”
- 对港股金融 live 字段，还会输出字段来源相关的口径警告
- 对非年报口径的快照，当前也会显式输出报告期警告，而不是静默把季度 / 中报结果与年报同行放在一起比较

评分结构补充：

- 当前第一版评分引擎只有“维度权重”，还没有“维度内子项配额/子项权重”这一层配置
- 这使得当前实现更容易扩模型、统一解释，但维度内部的重要性层次表达仍然偏弱
- 后续建议把核心维度逐步从“维度内平均制”演进到“分项配额制”
- 优先改造的维度应包括：`profit_quality`、`capital_safety_and_asset_quality`、`profitability_and_stability`、`valuation_fit`
- 改造目标不是推翻维度权重，而是在保留维度权重的前提下，为维度内关键指标再增加一层显式配额

完成标准：

- 输入一份标准 JSON 快照
- 输出总分、评级、优势、风险、缺失项
- 对异常字段和缺失字段有稳定处理

### 阶段 3：数据源接入

优先级建议：

1. 手工 JSON / CSV 导入
2. `akshare`
3. `tushare`
4. 其他外部源

完成标准：

- 至少一个数据源能稳定产出标准快照
- 数据源层和评分层解耦

当前仓库状态：这一阶段也已进入已落地状态，港股与 A 股公共快照入口已经分别沉到
`src/fundamental/data/hk_snapshot_fetcher.py` 与 `src/fundamental/data/cn_snapshot_fetcher.py`，并通过
`src/fundamental/services/` 提供抓取并分析入口。

当前已覆盖的行业桶和子模型，不再在本节重复展开，统一以 [fundamental-doc-map.md](fundamental-doc-map.md) 的“当前实现快照”为准。

补充说明：

- A 股银行 live 链路已经可跑通，`601328` 当前可通过公共源抓取并进入 `bank_v1` 分析
- 银行专属字段当前主要来自 Eastmoney A 股主指标表，例如 `NEWCAPITALADER`、`HXYJBCZL`、`NONPERLOAN`、`BLDKBBL`、`NET_INTEREST_MARGIN`
- 当前 THS A 股摘要表 schema 已经变化，代码侧已改为“列名优先，列位兜底”的解析方式
- 当前 A 股点位型 `dividend_yield` 暂无稳定公共源，因此依赖股息率准入的 CN 子模型目前统一在 live 分析链路里按运行时放宽处理
- 港股保险 / 券商的公共源仍只稳定提供通用盈利、估值、股息率等字段，尚未稳定提供 `solvency_adequacy_ratio`、`embedded_value_growth`、`new_business_value_growth`、`net_capital_ratio` 等监管型字段
- 因此 `insurance_v1` 与 `broker_v1` 的第一版现实路径已经变成：
	- 主源继续提供通用财务字段
	- 关键监管字段优先走公司官方披露 fallback
	- 剩余无法自动化的字段保留 `manual supplement`

阶段 3 的最新状态可以更具体地写成：

- `06886` 已能通过华泰官方年报 fallback 补齐 `net_capital_ratio` 代理值，并进入 `broker_v1` live 分析
- `01339` 已能通过人保官网偿付能力披露 fallback 补齐 `solvency_adequacy_ratio`
- `00175` 已形成 `auto_manufacturing_v1` live 闭环，并能自动补 `gross_margin`、`gross_margin_trend`、`overseas_revenue_share`、`price_war_pressure` 等汽车制造关注字段
- A 股公共快照已收紧为“优先最近年报期”的取数策略，避免维度评分混入季度口径
- 港股公共快照已收紧为“优先年报，必要时显式回退到最新可用期”，并对金融补充字段增加同报告期优先匹配
- 但 `insurance_v1` 其余字段如 `combined_ratio`、`investment_return`、`embedded_value_growth`、`new_business_value_growth` 仍未完全自动化，因此人保 live 完整报告仍主要依赖 `manual supplement`

### 阶段 4：与技术面联动

交付：

- 联合分析结果模型
- 技术面结果与基本面评分的整合逻辑
- 候选池过滤规则

完成标准：

- 可以对已有缠论分析结果追加基本面评级
- 可以标记“技术面成立但基本面不支持”的情况

当前仓库状态：这一阶段也已进入已落地状态，联合文本报告和微信群发送链路已经可用，并且：

- 联合报告会保留基本面 `警告` 段，提示字段来源口径
- 联合报告里的维度得分会附简版计算说明，减少“这个分是怎么算出来的”这类解释成本

### 阶段 5：批量分析与输出

交付：

- 批量输入
- 汇总表输出
- 与现有报告导出链路结合

当前仓库状态：这一阶段也已经部分落地。

- 基本面简报和 scorecard 已有批量回补入口
- blended 单标的入口已经接到现有 CLI
- CloudBase 发布主链路已经能调起单标的 mixed 报告重建、发布包构建与上传
- 组合级 `manifest` 审计文件也已形成固定落盘协议

但这阶段仍未完全收尾，主要还差：

- 更多批量脚本对 blended 参数的细粒度透传
- 更统一的汇总表 / 清单输出层
- CLI 与批量脚本之间更稳定的一致参数面

## 3. 当前模块分层

按当前仓库实现，基本面代码已经大体分为七层：

- `data`: 财报或指标数据获取，以及不同来源字段到标准快照的映射
- `models`: 标准快照与评分输出模型
- `config`: 子模型配置对象与注册表
- `validation`: 字段完整性与准入校验
- `scoring`: 基本面评分与风险标记
- `reporting`: 文本报告输出
- `services`: “抓取 + 分析”总入口

补充说明：`models/` 当前已经不只包含单期对象，也已经包含 blended 跨期对象；`services/` 当前也已经同时包含 HK/CN 单期与 blended 入口。

这比早期草案里的 `providers / normalizers / scoring / reporting` 更贴近当前实现，也更符合现在的导出接口。

## 4. 第一阶段必须避免的事情

- 把抓数逻辑和评分逻辑写在同一个脚本里
- 直接把某个网站的字段命名当成领域模型
- 没有定义缺失值规则就开始写 if/else
- 总分规则没冻结就开始做联合筛选
- 没有测试样例就引入大量外部依赖

## 5. 待确认问题

这些问题在继续扩模型和联动前仍然值得明确：

- 港股和 A 股是否使用完全相同的评分阈值
- 科技行业是否还需要继续细拆更多边界子模型
- 工业自动化与军工高端装备是否要在下一阶段分拆
- 能源资源行业是否先只覆盖煤炭、石油石化、有色
- 能源资源行业在第一版之后是否要继续细拆煤炭、石油石化、有色、黄金等子模型
- `pb`、`ps_ttm` 是否作为通用核心指标，还是只保留在估值扩展层
- 报告期冲突时，是否允许年报和 TTM 混合
- 多期趋势评分是否放到第二阶段还是第三阶段
- 当前评分结构是否继续维持“维度内平均制”，还是逐步升级到“分项配额制”

已明确事项：

- 金融股已经单独拆出子模型，而不是继续套用通用科技/制造口径
- 第一版金融子模型已经包括：`bank_v1`、`insurance_v1`、`broker_v1`
- 金融行业第一阶段的主要剩余问题不再是“要不要单独建模”，而是“如何补齐 live 数据源字段”
- 非金融扩展行业桶已经包括：`utility_operator_v1`、`digital_infra_v1`、`home_appliance_v1`
- 中航科工当前先复用 `industrial_automation_v1`，后续再决定是否单拆军工高端装备模型
- 当前仓库中的真实实现仍是“维度内平均制”；`分项配额制` 被视为下一阶段评分引擎升级方向，而不是已落地能力

## 6. 当前剩余优先级

最初的实现顺序已经大体完成。按当前仓库状态，更值得保留为后续优先级的是：

1. 继续收缩港股金融 live 里的 `manual supplement`
2. 继续把能源资源从简版参数模型推进到增强版
3. 把当前评分引擎从“维度内平均制”升级到更符合业务主次的“分项配额制”
4. 继续扩大 blended 在批量脚本、CLI 和更多子模型上的一致支持
5. 在不破坏现有公共入口的前提下继续沉淀更统一的汇总输出层

按当前仓库状态，早期主骨架阶段已经完成，当前可以明确认为已经落地的包括：

- 科技四个子模型已落地
- 金融三类子模型已落地
- 公用事业、数字基础设施、家电消费制造三个扩展行业桶已落地
- 能源资源第一版简版子模型已落地，并以 `601088` 作为代表标的挂接
- A 股银行 live 数据源已打通第一版
- 港股保险 / 券商仍需要第二数据源或更细粒度公开指标表支持
- CN / HK blended 单标的服务与文本输出已落地

截至 2026-05-11，路线图层面只保留三条最重要的未收尾方向：

1. 继续收缩港股金融 live 里的 `manual supplement`，降低字段口径歧义
2. 继续把能源资源从简版参数模型推进到带成本曲线、储量寿命和商品价格敏感度的增强版
3. 把当前评分引擎从“维度内平均制”升级到更符合业务主次的“分项配额制”

截至 2026-05-12，今天的收尾结论可以再补三条：

1. 报告期一致性已经从“文档问题”升级为“代码默认行为”，CN/HK 都优先年报，回退时必须显式留痕
2. 当前 live 报告已经能同时解释“字段来自哪里”和“这次评分基于哪类报告期”
3. 下一阶段如果继续扩 live 数据源，优先级应放在减少金融字段 fallback 与 `manual supplement`，而不是继续横向铺新行业桶

截至 2026-05-16，路线图层面新增一条已经进入“先文档冻结”的方向：

4. 当前 live 评分虽然已经做到“优先年报、回退留痕”，但仍没有解决“年报后新季报已披露、评分新鲜度滞后”的问题
5. 对这个问题，当前建议方向不是“年报总分 + 季报总分”的简单线性混分，而是“年报锚定分 + 季报刷新层”
6. 对应方案文档已新增：
	- [fundamental-interim-scoring-design.md](fundamental-interim-scoring-design.md)
	- [fundamental-interim-scoring-interface.md](fundamental-interim-scoring-interface.md)

截至 2026-05-31，这条 blended 方向已经从“方案文档”推进到“代码已落地、边界仍待扩展”的状态：

1. HK / CN 两条链路都已具备 blended 服务入口
2. blended 文本评分卡与 brief 落盘 helper 已可直接复用
3. CLI 已暴露 `--blended-cn` 与 `--blended-hk`
4. 当前剩余问题不再是“要不要做 blended”，而是“如何把更多参数、更多子模型和更多批量链路平滑接入”